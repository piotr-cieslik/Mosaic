<UserControl x:Class="mosaic.ui.ProgressNotification.ProgressNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:mosaic.ui.ProgressNotification"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance, Type=local:ProgressNotificationViewModel,
                                               IsDesignTimeCreatable=True}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding Status}" />
        <ProgressBar Grid.Row="1"
                     Height="5"
                     Maximum="{Binding Maximum}"
                     Value="{Binding Value}" />
    </Grid>
</UserControl>